package com.eshop.app.catalog.domain.entity;

/** Represents the approval status of a master catalog product. */
public enum MasterProductApprovalStatus {
    DRAFT,
    PENDING_APPROVAL,
    APPROVED,
    REJECTED
}
